export class MapLegendViewLogic {
  constructor(component) {
    this.component = component;
  }

  // This component is mostly static, so minimal logic is needed
  // The legend items are defined in the template
}
